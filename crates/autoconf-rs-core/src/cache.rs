//! Cache management for config.cache / config.status (stub).
//!
//! Current status: Phase 1 — stub.

#[derive(Default)]
pub struct Cache;

impl Cache {
    pub fn new() -> Self {
        Self
    }
}
