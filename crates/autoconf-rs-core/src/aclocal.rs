//! aclocal — aclocal.m4 generator (stub).
//!
//! Receipt family: AC.CLI.ACLOCAL.*
//! Current status: Phase 1 — stub.

#[derive(Default)]
pub struct Aclocal;

impl Aclocal {
    pub fn new() -> Self {
        Self
    }
    pub fn process(&self, _input: &str) -> Result<String, String> {
        Ok(String::new())
    }
}
